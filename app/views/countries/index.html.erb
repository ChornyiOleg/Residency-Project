<div class="container">
  <div class="row row-cols-2">
    <% @countries.each do |records| %>
      <div class="col">
        <dl>
          <dt> <%= records.name %> </dt>
        </dl>
      </div>
    <% end %>
  </div>
  <%== pagy_nav(@pagy) %>
  <%== pagy_info(@pagy) %>
</div>