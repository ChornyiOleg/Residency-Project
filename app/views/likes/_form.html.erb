<%= form_with model: [@residence, @residence.likes.build] do |f| %>
  <% if current_user %>
    <% if @like.present? %>
      <div class="actions">
        <div class="quote_btn">
          <button class="btn" type="submit">Remove from Liked ❤️</button>
        </div>
      </div>
    <% else %>
      <div class="actions">
        <div class="quote_btn">
          <button class="btn" type="submit">Add to Liked 🖤</button>
        </div>
      </div>
    <% end %>
  <% else %>
    <%= link_to "Sign Up", new_author_path %> or
    <%= link_to "Log In", new_session_path %>
  <% end %>
<% end %>